# PythonPrimeCode
#This code is used for school students who just want to get through class 
#simple code that generates a list ~ and checks if the list is prime numbers or not

import random


def isPrime(i):
    for p in range(2, i - 1):
        if i % p == 0:
            flag = 1
            break
        else:
            flag = 0
            if flag == 0:
                print(i)

x = []
for i in range(1, 11):
    x.append(random.randint(1, 500))
print(x)
for i in x:
    isPrime(i)
